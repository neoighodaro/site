{
	"require": {
        "mouf/nodejs-installer": "~1.0"
    },
    "extra": {
        "mouf": {
            "nodejs": {
            	"version": "4.2.1"
            }
        }
    },
    "scripts": {
	    "post-install-cmd": [
	    	"vendor/bin/node -v && vendor/bin/npm -v",
	    	"vendor/bin/npm install bower",
	    	"vendor/bin/npm install",
	    	"./bower install",
	    	"node_modules/.bin/gulp production --production"
	    ]
	}
}